{
  "name": "client",
  "version": "1.0.0",
  "description": "TextBound Castle Crawl Frontend",
  "type": "module",
  "scripts": {
    "start": "parcel serve index.html --open --port 1234",
    "build": "parcel build index.html --dist-dir dist",
    "check-types": "tsc --noEmit",
    "test": "echo \"No client tests specified for now.\" && exit 0"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "typescript": "^5.4.5"
  },
  "author": "Walter Conley",
  "license": "ISC"
}
